server:
  port: 9002

spring:
  application:
    name: EMPLOYEE-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    eureka-service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
